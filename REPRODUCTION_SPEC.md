# Patent Classification Viewer 再現仕様書

## 1. 目的

日本の特許実務で使う分類コードを、静的ウェブアプリ上で確認する。

対象分類は以下の4種類。

- IPC
- FI
- CPC
- Fターム

このアプリは、ユーザーが入力したコードについて、次の2種類の表示を行う。

- コード情報表示
- 1つ下の階層表示

また、結果カード上でホバーしたときのツールチップ表示、およびクリック時に別ウィンドウで開く詳細表示を持つ。

## 2. 実装方針

- 完全な静的サイトとして実装する
- サーバーサイド処理は持たない
- フレームワークは使わず、`HTML + CSS + Vanilla JavaScript` で構成する
- GitHub Pages やローカル `file://` で動作すること
- `fetch(.json)` が失敗する `file://` 環境に備え、`<script src="...js">` によるフォールバックを持つこと

## 3. ファイル構成

最低限、以下の構成を維持する。

- `index.html`
- `app.js`
- `classification-common.js`
- `theme-utils.js`
- `fterm-utils.js`
- `detail.html`
- `detail.js`
- `styles.css`
- `data/`
- `scripts/build-fterm-data.js`

## 4. データセット

### 4.1 IPC / FI / CPC

`data/` 配下に分類別の shard データを持つ。

- `ipc-shard-*.json` と `ipc-shard-*.js`
- `fi-shard-*.json` と `fi-shard-*.js`
- `cpc-shard-*.json` と `cpc-shard-*.js`

加えて、各分類の index データを持つ場合がある。

### 4.2 テーマコード

FI と Fターム関連表示のため、以下を持つ。

- `data/theme-codes.json`
- `data/theme-codes.js`

テーマコードの各要素は少なくとも以下の情報を持つ。

- `themeCode`
- `type`
- `name`
- `coverage`

### 4.3 Fターム

Fタームはテーマコードごとに分割したデータを持つ。

- `data/fterm-term-<THEME>.json`
- `data/fterm-term-<THEME>.js`

例:

- `fterm-term-5C062.json`
- `fterm-term-2C062.json`

テーマ数は現状 1992 件で、`.json` と `.js` を合わせると 3984 ファイル存在する。

### 4.4 Fターム項目データ構造

`entries[code]` に以下を持つ。

- `code`
- `ja`
- `en`
- `parent`
- `level`
- `themeCode`
- `termCode`
- `themeType`
- `coverage` または `fiCoverage`

重要:

- テーマコード本体の root 項目も `entries` に含める
- `LL00` や `AA00` のような root branch 項目も `entries` に含める
- `RA01` などの個別 Fターム項目には、その項目に対応する FI 範囲または FI コードを `fiCoverage` として持たせる

例:

- `2C062RA01` -> `fiCoverage: "B41J3/60"`

## 5. UI 仕様

## 5.1 トップページ

ページ上部に以下を表示する。

- 右上リンク: 分類対照ツール
- 英字の eyebrow: `Classification Hierarchy Viewer`
- 日本語タイトル
- 日本語リード文

右上リンク先:

- [分類対照ツール](https://www.jpo.go.jp/cgi/cgi-bin/search-portal/narabe_tool/narabe.cgi)

## 5.2 表示モード

ラジオボタンではなく、チップ状の 2 択 UI にする。

- `コード情報`
- `1つ下の階層`

## 5.3 参照する分類

チップ状の 4 択 UI にする。

- `IPC`
- `FI`
- `CPC`
- `Fターム`

文言はモードによって変える。

- コード情報モード: `参照する分類`
- 1つ下の階層モード: `1つ下の階層で参照する分類`

## 5.4 入力欄

- `textarea` を使う
- 複数コード入力に対応する
- 縦並び入力と横並び入力の両方に対応する
- 全角英数字、全角スラッシュ、全角スペースを受け付ける
- 内部で `NFKC` 正規化する

プレースホルダー例:

- `H04N1/029`
- `H04N1/00C`
- `H04N1/00061`
- `5C062`
- `5C062AB21`

## 5.5 サンプルボタン

以下のサンプルを表示する。

- `H04N1/029`
- `H04N1/00C`
- `H04N1/00061`
- `5C062`

## 5.6 実行ボタン

ボタン文言は `表示する`。

ただし、入力時には自動検索も行う。

## 6. 入力正規化ルール

## 6.1 共通

以下を内部正規化する。

- `NFKC` 正規化
- 英字は大文字化
- 改行、タブ、半角スペース、全角スペース除去
- 不要な句読点や特殊なスラッシュ類の除去

## 6.2 IPC / FI / CPC

次のような入力を許容する。

- `H04N1/029`
- `H04N 1/029`
- `H04N1/00 567 H`
- `G06F 3/12,309`
- `H04N1/00,002A`
- `B41J29`
- `B41J29/`

### 6.2.1 `/00` 補完

次のような入力は、内部的に `/00` を補完する。

- `B41J29` -> `B41J29/00`
- `B41J29/` -> `B41J29/00`

### 6.2.2 FI のバックスラッシュ

FI の anchor 表記として末尾 `\` を持つコードを許容する。

- 表示上は `\` を出さない
- 内部 lookup 時には `code` と `code + "\"` を両方試す

## 6.3 Fターム

次を許容する。

- `5C062`
- `5C062AB21`

Fターム正規表現:

- 5桁のテーマコード: `^[0-9][A-Z][0-9]{3}$`
- 個別 Fターム: `^[0-9][A-Z][0-9]{3}[A-Z]{2}[0-9]{2}$`

## 7. 自動判定ルール

コード情報モードでは、入力内容から Fタームを自動判定する。

ルール:

- ユーザーが明示的に `Fターム` を選択していれば Fタームとして処理する
- そうでなくても、入力が Fタームパターンに一致し、かつ IPC/FI/CPC のヘッドに見えない場合は Fタームとして扱う

例:

- `2C061` -> Fターム
- `5C062AB21` -> Fターム

## 8. コード情報モード

## 8.1 概要

入力された 1 件以上のコードそれぞれについて、分類情報カードを表示する。

## 8.2 表示内容

各カードに以下を表示する。

- 分類コード
- 分類種別タグ
- 階層情報タグ
- 定義
- 補足情報ブロック

## 8.3 階層情報表示

階層情報は `・` の個数のみで表す。

- `階層:` というラベルは出さない
- 最上位を示す特別な文言は出さない
- 空なら階層タグ自体を隠す

### 8.3.1 IPC / FI / CPC

親をたどって root までの深さを数える。

### 8.3.2 Fターム

Fタームだけは見かけ上の深さを 1 段差し引く。

目的:

- テーマコード直下の `AA00` や `LL00` を `・なし` とする

期待値:

- `2C061` -> 空
- `2C061LL00` -> 空
- `2C061LL01` -> `・`
- `2C061LL02` -> `・・`

## 8.4 定義表示ルール

### IPC

- 日本語定義のみ表示
- 英語は非表示
- 日本語が空の場合、日本語ペイン自体を隠してよい

### FI

- 日本語定義のみ表示
- 英語は非表示

### CPC

- 日本語と英語の両方を表示
- 2 カラムレイアウトでもよい

### Fターム

- 日本語定義のみ表示
- 英語は非表示

## 8.5 FI の補足表示

FI のときは `対応テーマコード` ブロックを表示する。

各テーマ項目には以下を表示する。

- テーマコード + 種別
- テーマ名
- 対応 FI 範囲

テーマコードのテキスト自体にリンクを張る。

リンク先:

- `https://www.j-platpat.inpit.go.jp/cache/classify/patent/PMGS_HTML/jpp/F_TERM/ja/fTermList/fTermList<themeCode>.html`

例:

- `5C062 / F I`

`Fタームリストを開く` のような別文言リンクは出さない。

## 8.6 Fタームの補足表示

Fタームのときは `対応FI` ブロックを表示する。

表示対象:

- ユーザーが直接入力した Fターム結果カードのみ
- 1つ下の階層モードで表示される子 Fタームカードには表示しない

表示内容:

- 対応する FI または FI 範囲
- テーマコード / テーマ種別
- テーマ名

表示優先順位:

1. 個別 Fターム項目の `fiCoverage`
2. テーマ root の `fiCoverage`
3. テーマ root の `coverage`

例:

- `2C062RA01` -> `B41J3/60`

## 9. 1つ下の階層モード

## 9.1 概要

ユーザーが指定したコードの immediate children のみを一覧表示する。

孫以下は表示しない。

## 9.2 参照分類

1つ下の階層モードでは、ユーザーが `IPC / FI / CPC / Fターム` のどれで子階層を見たいかを明示的に選ぶ。

理由:

- 同じ `H04N1/04` でも、IPC と FI と CPC では子階層が異なるため

## 9.3 結果構造

1入力コードごとに以下を表示する。

- 入力コード見出し
- 一致件数
- 親カード
- 子カード一覧

## 9.4 テーマコード表示

このモードでは、子カードごとに `対応テーマコード` や `対応FI` を出さない。

ただし、親カードには以下を出してよい。

- 親が FI なら `対応テーマコード`
- 親が Fタームなら `対応FI`

## 10. ツールチップ

## 10.1 基本

結果カード内の分類コードにホバーすると、`title` 属性のツールチップを出す。

カスタム黒ポップアップは使わない。

## 10.2 コード情報モード

ツールチップには上位階層の一覧を表示する。

- 最上位から対象コードまで順に並べる
- 最後に、ホバー中のコード自身も含める
- ホバー中のコードは最下段に出る

各行の形式:

- `<階層の点列> : <コード> : <定義>`

## 10.3 1つ下の階層モード

ツールチップには immediate children の一覧を表示する。

- 上位階層は出さない
- immediate children をすべて列挙する
- immediate children がなければ、その旨のメッセージを出す

## 11. クリック時の詳細ページ

分類コードをクリックすると `detail.html` を新規ウィンドウで開く。

クエリパラメータ:

- `code`
- `mode`
- `overlay`

例:

- `detail.html?code=G06F3%2F12301%5C&mode=fi&overlay=children`

## 11.1 ancestor モード

`overlay=ancestors`

表示:

- タイトル: 上位階層情報を確認する
- リード文: 選択した分類コードについて、上位階層をルートまで表示します。
- 対象コードまでの lineage を root から順に表示

FI / Fタームの補足ブロックは、lineage の最後のカードにのみ表示する。

## 11.2 children モード

`overlay=children`

表示:

- タイトル: 1つ下の階層情報を確認する
- リード文: 選択した分類コードについて、1つ下の階層を一覧表示します。
- セクション見出し: 1つ下の階層

表示構造:

- 親カード 1件
- immediate children のカード一覧

子カードにテーマコードや対応 FI を付けない。

## 12. 複数コード入力

コード情報モードでは複数コードを受け付ける。

入力パターン:

- 改行区切り
- 半角空白区切り
- 全角空白区切り
- ShareResearch 形式の FI 記法

ShareResearch 形式の例:

- `G06F 3/12,309`
- `H04N1/00,002A`

この場合、前半の IPC ヘッドを引き継いで完全な FI コードへ補完すること。

## 13. ステータス表示

ステータス行を常時持つ。

状態:

- 通常
- success
- error

例:

- `1件を表示しています。`
- `FI で 1件の分類コードについて、1つ下の階層を表示しています。`
- `一致する分類コードが見つかりませんでした。未検出: 2C061`

## 14. 詳細なロジック仕様

## 14.1 共通データローダ

`classification-common.js` に共通関数を集約する。

主な責務:

- shard 読み込み
- 親子関係探索
- 深さ計算
- `/00` 補完
- FI の `\` 付き anchor lookup
- 結果カードモデル生成
- ツールチップ文字列生成
- 結果カード DOM 生成

## 14.2 Fタームローダ

`fterm-utils.js` に以下を持つ。

- `normalizeFtermCode(value)`
- `getThemeCode(code)`
- `loadFtermDataset(code)`

Fタームのデータファイル名は、先頭5文字のテーマコードで決める。

例:

- `2C062RA01` -> `2C062` -> `./data/fterm-term-2C062.json`

## 14.3 テーマコード照合

`theme-utils.js` では、FI とテーマコードの対応判定を行う。

役割:

- FI コードの比較
- coverage 範囲の parsing
- theme-codes データとの照合

## 14.4 Fタームデータ生成スクリプト

`scripts/build-fterm-data.js` は J-PlatPat の Fターム HTML を取得してデータ化する。

入力:

- `data/theme-codes.json`

取得元:

- `https://www.j-platpat.inpit.go.jp/cache/classify/patent/PMGS_HTML/jpp/F_TERM/ja/fTermList/fTermList<themeCode>.html`

出力:

- `data/fterm-term-<themeCode>.json`
- `data/fterm-term-<themeCode>.js`

生成時に行うこと:

- root theme 項目生成
- root branch 項目生成
- bullet count に基づく parent/level 付与
- 各ブロックの FI 対応情報を `fiCoverage` として保持

## 15. デザイン仕様

- 背景は暖色系グラデーション
- パネルは白に近い半透明
- 角丸を大きめにする
- タグは丸ピル形状
- 強い赤茶系アクセントカラーを使う
- フォントは和文 UI 系、見出しは明朝系

## 16. 動作環境

以下で動くこと。

- ローカル `file://`
- GitHub Pages

`file://` では JSON fetch が失敗しうるため、`.js` フォールバック必須。

## 17. キャッシュ対策

`index.html` と `detail.html` から読み込む CSS / JS には query version を付ける。

現状例:

- `?v=20260429r1`

## 18. 受け入れテスト

最低限、以下を満たすこと。

- `H04N1/029` を IPC で検索できる
- `H04N1/00C` を FI で検索できる
- `H04N1/00061` を CPC で検索できる
- `5C062` を Fタームで検索できる
- `5C062AB21` を Fタームで検索できる
- `B41J29` が `/00` 補完で検索できる
- `B41J29/` が `/00` 補完で検索できる
- `Ｇ０６Ｆ　３／１２` のような全角入力が検索できる
- `G06F 3/12,309` が FI として解釈できる
- `H04N1/04` を FI の 1つ下の階層で開くと `H04N1/04B` などが出る
- Fターム `2C061LL01` の階層表示が `・` になる
- Fターム `2C062RA01` の対応FIとして `B41J3/60` が出る
- 1つ下の階層モードで表示された子カードには、テーマコードや対応FIを出さない

## 19. 非機能要件

- 入力後の自動検索は過敏すぎないよう debounce する
- 目安 350ms 程度
- 画面は PC 優先だが、狭い幅でも崩れすぎないこと
- エラー時にアプリ全体が落ちず、ステータス行に理由を出すこと
