# 他AIへ渡す再現用プロンプト

以下を、そのまま別のAIや別スレッドへ渡してよい。

---

あなたは、既存の静的ウェブアプリを完全再現する実装担当です。

以下の要件に従って、**まったく同一の動作をするウェブアプリ**を作成してください。

## 目的

日本の特許分類を検索・確認する静的ウェブアプリを作る。

対象分類:

- IPC
- FI
- CPC
- Fターム

## 実装方式

- フレームワークは禁止
- `HTML + CSS + Vanilla JavaScript` のみ
- GitHub Pages と `file://` の両方で動作させる
- `fetch(.json)` が使えない `file://` 環境向けに、`.js` ファイル読み込みフォールバックも必須

## 必須ファイル

- `index.html`
- `app.js`
- `classification-common.js`
- `theme-utils.js`
- `fterm-utils.js`
- `detail.html`
- `detail.js`
- `styles.css`
- `data/`

## 画面仕様

トップ画面には以下を置く。

- 右上に「分類対照ツール」への外部リンク
- 英字の eyebrow: `Classification Hierarchy Viewer`
- 日本語タイトル
- 日本語リード文
- 表示モード切替チップ
  - `コード情報`
  - `1つ下の階層`
- 参照分類切替チップ
  - `IPC`
  - `FI`
  - `CPC`
  - `Fターム`
- 複数行 `textarea`
- `表示する` ボタン
- サンプルボタン
  - `H04N1/029`
  - `H04N1/00C`
  - `H04N1/00061`
  - `5C062`
- ステータス表示
- 検索結果表示エリア

## 入力仕様

### 共通

- 全角英数字、全角スラッシュ、全角スペースを受け付ける
- 内部では `NFKC` 正規化
- 英字は大文字化
- 不要な空白や改行を除去

### IPC / FI / CPC

以下のような表記ゆれを受け付けること。

- `H04N1/029`
- `H04N 1/029`
- `H04N 1/00 567 H`
- `G06F 3/12,309`
- `H04N1/00,002A`
- `B41J29`
- `B41J29/`

`/` の後が省略された場合は `/00` 補完すること。

- `B41J29` -> `B41J29/00`
- `B41J29/` -> `B41J29/00`

FI の anchor 表記で末尾 `\` が付くデータもあるので、lookup 時には `code` と `code + "\"` を両方試すこと。

### Fターム

以下に対応すること。

- `5C062`
- `5C062AB21`

また、コード情報モードでは Fタームを自動判定すること。

## 表示モード仕様

### 1. コード情報

入力された各コードについて、分類コード情報カードを表示する。

カードには以下を表示する。

- 分類コード
- 分類種別タグ
- 階層情報タグ
- 定義
- FI または Fタームに関する補足情報ブロック

### 2. 1つ下の階層

ユーザーが指定したコードについて、immediate children のみ表示する。

- 孫以下は表示しない
- このモードでは、どの分類体系で子階層を見るかをユーザーが明示的に選ぶ
- 親カードは表示してよい
- 子カードにはテーマコードや対応FIの補足表示を付けない

## 定義表示ルール

- IPC: 日本語のみ
- FI: 日本語のみ
- CPC: 日本語 + 英語の両方
- Fターム: 日本語のみ

## 階層表示ルール

階層情報は `・` の個数のみで表す。

- `階層:` のようなラベルは出さない
- 最上位を表す特別な文言も出さない

### Fタームだけの特別ルール

Fタームは見かけ上の深さを 1 段引いて表示すること。

期待値:

- `2C061` -> 空
- `2C061LL00` -> 空
- `2C061LL01` -> `・`
- `2C061LL02` -> `・・`

## ツールチップ仕様

分類コード上にホバーしたとき、`title` 属性の標準ツールチップだけを使うこと。

黒いカスタムポップアップは作らないこと。

### コード情報モード

ツールチップには上位階層を最上位から順に表示し、最後にホバー中のコード自身も表示すること。

### 1つ下の階層モード

ツールチップには immediate children の一覧を表示すること。

## クリック時の詳細ページ

分類コードをクリックすると `detail.html` を別ウィンドウで開くこと。

パラメータ:

- `code`
- `mode`
- `overlay`

### `overlay=ancestors`

- 選択コードの上位階層を root まで表示

### `overlay=children`

- 選択コードの 1つ下の階層を表示
- さらにそこから別コードをクリックすると、そのコードの 1つ下の階層ページへ遷移できること

## FI のテーマコード表示

FI のコード情報表示では `対応テーマコード` を表示すること。

各テーマカードには以下を表示する。

- テーマコード + 種別
- テーマ名
- 対応 FI 範囲

テーマコードそのものにリンクを張ること。

リンク先:

- `https://www.j-platpat.inpit.go.jp/cache/classify/patent/PMGS_HTML/jpp/F_TERM/ja/fTermList/fTermList<themeCode>.html`

`Fタームリストを開く` という別ボタンや別文言リンクは不要。

## Fタームの対応FI表示

Fタームのコード情報表示では `対応FI` ブロックを表示すること。

表示対象は、**ユーザーが直接入力した Fターム結果カードのみ** とする。

- 1つ下の階層モードで表示される子 Fタームカードには出さない

表示優先順位:

1. 個別 Fターム項目の `fiCoverage`
2. テーマ root 項目の `fiCoverage`
3. テーマ root 項目の `coverage`

例:

- `2C062RA01` -> `B41J3/60`

## Fタームデータの扱い

Fタームはテーマコード単位で分割データを持つこと。

ファイル名:

- `data/fterm-term-<THEME>.json`
- `data/fterm-term-<THEME>.js`

例:

- `fterm-term-2C062.json`

Fタームごとのデータ項目は少なくとも以下を持つこと。

- `code`
- `ja`
- `parent`
- `level`
- `themeCode`
- `termCode`
- `themeType`
- `fiCoverage`

## Fターム生成スクリプト

可能なら、J-PlatPat の Fターム HTML からデータを生成するスクリプトも用意すること。

参照元:

- `https://www.j-platpat.inpit.go.jp/cache/classify/patent/PMGS_HTML/jpp/F_TERM/ja/fTermList/fTermList<themeCode>.html`

特に、各 Fターム項目に対応する FI は、ページ中の該当ブロックから抽出して `fiCoverage` に入れること。

例:

- `2C062RA01` -> `B41J3/60`

## デザイン

- 暖色系の落ち着いた背景グラデーション
- 半透明の白いパネル
- 大きめの角丸
- 赤茶系のアクセントカラー
- タグは丸い pill 形状
- 日本語 UI に自然な見た目

## 動作要件

以下を満たすこと。

- ローカル `file://` で動く
- GitHub Pages で動く
- キャッシュ対策として CSS / JS の query version を付ける
- 入力時に自動検索する
- ただし debounce を入れる
- 目安は 350ms

## 受け入れテスト

最低限、以下が成立すること。

- `H04N1/029` を IPC として検索できる
- `H04N1/00C` を FI として検索できる
- `H04N1/00061` を CPC として検索できる
- `5C062` を Fタームとして検索できる
- `5C062AB21` を Fタームとして検索できる
- `B41J29` が `/00` 補完される
- `Ｇ０６Ｆ　３／１２` が正規化される
- `G06F 3/12,309` が FI として解釈できる
- `2C061LL01` の階層表示が `・`
- `2C062RA01` の対応FIが `B41J3/60`
- 1つ下の階層モードの子カードにはテーマコードや対応FIを出さない

## 追加指示

- 既存挙動を変えずに再現すること
- 不要な UI の追加はしないこと
- 文言や表示ロジックを勝手に簡略化しないこと
- 実装後は、上の受け入れテストに沿って自己確認すること

---
